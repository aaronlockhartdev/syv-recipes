# Pinned to the reference install (vllm 0.27.1 -> torch 2.13.0 cu130,
# triton 3.7.1, flashinfer-python 0.6.16.post3; the rest pinned to what the
# reference venv resolved). huggingface_hub + hf_transfer are what the
# prepare scripts use to download through HF's built-in cache.
vllm==0.27.1
transformers==5.15.0
tokenizers==0.22.2
compressed-tensors==0.17.0
huggingface_hub==1.27.0
hf_transfer==0.1.9
ninja==1.13.0
# required by --prefix-caching-hash-algo xxhash in both recipes
xxhash==4.0.1
